import random
mt = ['zombie', 'goblin', 'dragon']

class Monster:
    def __init__(self, x==18, y=='Anthy', z=='male', b=="100"):
        self.age = x
        self.name = y
        self.gender = z
        self.hitpoint = b
        if len(self.name) > 4
            self.type = 'zombie'
        else
            self.type = 'goblin'


    def readout(self):
        print(self.name)
        print(self.age)
        print(self.gender)
        print(self.type)
        print(self.hitpoint)

c=Monster()
c.readout()
